# Google Authentication Configuration
# Get credentials from Google Cloud Console: https://console.cloud.google.com/
#
# Choose ONE of the two authentication methods below:

# ============================================================
# METHOD 1: Service Account (Recommended for CLI/Automation)
# ============================================================
# Instructions:
# 1. Create a new project or select an existing one
# 2. Enable the Google Docs API
# 3. Create a Service Account
# 4. Download the JSON key file
# 5. Set the path to the key file below
# 6. Share your Google Docs with the service account email

GOOGLE_SERVICE_ACCOUNT_KEY_FILE=/path/to/your-service-account-key.json

# ============================================================
# METHOD 2: OAuth 2.0 (User Credentials)
# ============================================================
# Instructions:
# 1. Create a new project or select an existing one
# 2. Enable the Google Docs API
# 3. Create OAuth 2.0 credentials (Desktop app type)
# 4. Copy the client ID and client secret below
# 5. First run will open a browser for authentication

# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret

# Note: If GOOGLE_SERVICE_ACCOUNT_KEY_FILE is set, it takes precedence over OAuth credentials
